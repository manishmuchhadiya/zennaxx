import React from "react";
import style from "./card.module.css";

const Card = () => {
  return (
    <div>
      <button type="submit" className={style.btn}>
        done
      </button>
    </div>
  );
};

export default Card;
