import React from "react";

const Navbar = () => {
  const navbar = "Navigation bar";
  return (
    <div>
      <div className="nav">
        <h4>This is {navbar}</h4>
      </div>
    </div>
  );
};

export default Navbar;
